## Human IAM Roles

Owner
- Used for project-level control and emergencies

Compute Instance Admin (v1)
- Required for VM, instance template, and instance group operations

Service Account User
- Allows attaching service accounts to VMs

This separation ensures secure and auditable access.